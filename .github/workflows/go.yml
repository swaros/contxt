### This file is generated by using ./imports/go_build.tpl
### Any change you are doing here is overwritten while the next contxt build
### change go_build.tpl instead
name: Go

on:
  push:
    branches: [ main  ]
  pull_request:
    branches: [ wip/release** ]

jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:

    - name: Set up Go 1.x
      uses: actions/setup-go@v3
      with:
        go-version: ^1.18
      id: go

    - name: Check out code into the Go module directory
      uses: actions/checkout@v3
    - name: Build-release
      run: go build -ldflags " -X github.com/swaros/contxt/module/configure.minversion=3 -X github.com/swaros/contxt/module/configure.midversion=5 -X github.com/swaros/contxt/module/configure.mainversion=0 -X github.com/swaros/contxt/module/configure.build=.20241001.060114-linux-release" -o ./bin/contxt cmd/cmd-contxt/main.go

    - name: Test
      run: |
          go test  -failfast ./module/yacl/./...
          go test  -failfast ./module/yamc/./...
          go test  -failfast ./module/runner/./...
          go test  -failfast ./module/ctxtcell/./...
          go test  -failfast ./module/configure/./...
          go test  -failfast ./module/dirhandle/./...
          go test  -failfast ./module/systools/./...
          go test  -failfast ./module/trigger/./...
          go test  -failfast ./module/linehack/./...
          go test  -failfast ./module/ctemplate/./...
          go test  -failfast ./module/ctxout/./...
          go test  -failfast ./module/taskrun/./...
          go test  -failfast ./module/awaitgroup/./...
          go test  -failfast ./module/shellcmd/./...
          go test  -failfast ./module/ctxshell/./...
          go test  -failfast ./module/tasks/./...
          go test  -failfast ./module/yaclint/./...
          go test  -failfast ./module/mimiclog/./...
          go test  -failfast ./module/process/./...
    - name: contxt-artifact-release
      uses: actions/upload-artifact@v3
      with:
        name: "contxt-release"
        path: ./bin/contxt
