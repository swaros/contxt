--- 
task: 
  - id: build
    script: 
      - rm -rf dist/
      - goreleaser build --skip-validate --rm-dist
      - cd dist/contxt_linux_amd64 && ./contxt version
  - id: init
    script: 
      - "git status"
