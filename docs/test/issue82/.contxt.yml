config:
  imports:
    - docker-inspect.json json_a

task:
  - id: issue82
    script:
      - "#@export-to-json json_a IMAGE-INFO"

  - id: echo
    needs:
      - issue82
    script:
      - echo "${IMAGE-INFO}"
